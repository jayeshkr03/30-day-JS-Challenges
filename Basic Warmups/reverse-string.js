// This function takes a string as input and returns the string reversed.
function reverseString(str) {
    return str.split('').reverse().join(''); // Split the string into an array of characters, reverse it, and join it back into a string
}
console.log(reverseString("JavaScript")); 

// Output:
// tpircSavaJ